#include <stdio.h>

int main()
{
    int otp = 1234;
    int user_otp;

    printf("Enter OTP: ");
    scanf("%d", &user_otp);

    if(user_otp == otp)
        printf("OTP Verified Successfully");
    else
        printf("Invalid OTP");

    return 0;
}
