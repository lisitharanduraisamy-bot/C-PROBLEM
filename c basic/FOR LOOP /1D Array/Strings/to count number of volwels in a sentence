#include <stdio.h>

int main() {

    char str[100];
    fgets(str, 100, stdin);
    int count = 0;
    for (int i=0;str[i]!='\0';i++) {
        if (str[i]=='a' || str[i]=='e' || str[i]=='i' || str[i]=='o') {
            count++;
        }
    }
    printf("%d\n",count);

    return 0;
}

