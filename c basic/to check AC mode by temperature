#include <stdio.h>

int main() {
    int temperature;
    scanf("%d",&temperature);
    if (temperature<20) {
        printf("Heating Mode");
    }else if (temperature>=20 && temperature<=30) {
        printf("Normal Mode");
    }else {
        printf("Cooling Mode");
    }

    return 0;
}
